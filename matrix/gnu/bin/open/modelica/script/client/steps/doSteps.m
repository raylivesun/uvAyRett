#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#if 0
/// @brief 
/// @param cref 
/// @return 
void oms_status_enu_t oms_doStep(const char* cref) {
    // Simulating the OMS step operation
    // Replace with actual implementation based on the OMS library
    // ...
    // Perform the step operation
    // ...

    // Example implementation: Print a message indicating the step operation
    printf("Step operation performed with reference: %s\n", cref);

    // Return the status of the step operation
    return OMS_STATUS_OK;
}

void oms_status_enu_t oms_doStep(const char* cref) {
    // Simulating the OMS step operation
    // Replace with actual implementation based on the OMS library
    // ...
    // Perform the step operation
    // ...

    // Example implementation: Print a message indicating the step operation
    printf("Step operation performed with reference: %s\n", cref);

    // Return the status of the step operation
    return OMS_STATUS_OK;
}



int main() {
    // Test the oms_doStep function with different reference strings
    const char* ref1 = "ref1";
    const char* ref2 = "ref2";

    oms_doStep(ref1);
    oms_doStep(ref2);

    return 0;
}
#endif